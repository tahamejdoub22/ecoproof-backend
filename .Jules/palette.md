## 2026-01-26 - Swagger UI DX
**Learning:** Enabling search (`filter: true`) and collapsing tags (`docExpansion: 'none'`) in Swagger UI significantly improves developer experience by making large APIs easier to navigate.
**Action:** Always configure `swaggerOptions` with these defaults in `src/main.ts` for NestJS projects.
